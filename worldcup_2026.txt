<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="theme-color" content="#ffffff" />
    <title>World Cup 2026 Results</title>
    <!-- Inline PWA Manifest -->
    <link rel="manifest" id="manifest" />
    <script>
      const manifest = {
        name: "World Cup 2026 Results",
        short_name: "WC2026",
        start_url: "/",
        display: "standalone",
        theme_color: "#ffffff",
        background_color: "#f9fafb",
        icons: [{ src: "https://flagcdn.com/192x144/us.png", sizes: "192x192", type: "image/png" }]
      };
      const stringManifest = JSON.stringify(manifest);
      const blob = new Blob([stringManifest], {type: 'application/json'});
      const manifestURL = URL.createObjectURL(blob);
      document.querySelector('#manifest').setAttribute('href', manifestURL);
    </script>
  </head>
  <body class="bg-gray-50 text-gray-900 antialiased">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
    <script>
      // Register stub service worker for PWA installability
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
          navigator.serviceWorker.register('/sw.js').catch(() => {
            // Ignore failure in preview
          });
        });
      }
    </script>
  </body>
</html>
